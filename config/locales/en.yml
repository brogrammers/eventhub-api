en:
  activerecord:
    errors:
      models:
        location:
          attributes:
            latitude:
              less_than: &invalid "'%{value}' is invalid"
              greater_than: *invalid
            longitude:
              less_than: *invalid
              greater_than: *invalid